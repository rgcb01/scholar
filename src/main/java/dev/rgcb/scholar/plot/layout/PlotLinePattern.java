package dev.rgcb.scholar.plot.layout;

/** Derived line pattern used to distinguish plot series without relying only on color. */
public enum PlotLinePattern {
    SOLID,
    DASHED,
    DOTTED,
    DASH_DOT,
    LONG_DASH,
    DENSE_DOT
}
