package dev.rgcb.scholar.editor;

/** Broad presentation category for discovery; it does not control action behavior. */
public enum EditorActionCategory {
    FILE,
    EDIT,
    INSERT,
    FORMAT,
    VIEW,
    DATA,
    TABLE,
    PLOT,
    FIGURE,
    DIAGRAM,
    LAYOUT
}
